import { Module } from '@nestjs/common';

// Swagger setup is done in main.ts via setupSwagger()
// This module exists only for NX library structure consistency
@Module({})
export class SwaggerConfigModule {}
